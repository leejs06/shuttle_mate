<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="join">

    <!-- 연령대 리스트 전체 조회 -->
    <select id="selectUserAgeGroup" resultType="hashmap">
    <![CDATA[
        SELECT  CodeValue AS code,
                CASE WHEN CodeView IS NOT NULL THEN CodeView
                     ELSE CodeName
                    END AS value
        FROM tb_code
        WHERE 1=1
          AND GroupCode = #{groupCode}
          AND UseYN = 'Y'
        ORDER BY Array ASC
    ]]>
    </select>

    <!-- 전국 급수 리스트 전체 조회 -->
    <select id="selectAddr1Level" resultType="hashmap">
    <![CDATA[
        SELECT  CodeValue AS code,
                CASE WHEN CodeView IS NOT NULL THEN CodeView
                     ELSE CodeName
                    END AS value
        FROM tb_code
        WHERE 1=1
          AND GroupCode = #{groupCode}
          AND UseYN = 'Y'
        ORDER BY Array ASC
    ]]>
    </select>

    <!-- 시 급수 리스트 전체 조회 -->
    <select id="selectAddr2Level" resultType="hashmap">
    <![CDATA[
        SELECT CodeValue AS code,
                CASE WHEN CodeView IS NOT NULL THEN CodeView
                    ELSE CodeName
                    END AS value
        FROM tb_code
        WHERE 1=1
            AND GroupCode = #{groupCode}
            AND UseYN = 'Y'
        ORDER BY Array ASC
    ]]>
    </select>

    <!-- 구 급수 리스트 전체 조회 -->
    <select id="selectAddr3Level" resultType="hashmap">
    <![CDATA[
        SELECT CodeValue AS code,
               CASE WHEN CodeView IS NOT NULL THEN CodeView
                    ELSE CodeName
                   END AS value
        FROM tb_code
        WHERE 1=1
          AND GroupCode = #{groupCode}
          AND UseYN = 'Y'
        ORDER BY Array ASC
    ]]>
    </select>
</mapper>